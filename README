== Tool to download latest source ==

Usage:

    ./srcget.sh <name>

i.e.:

    ./srcget.sh zlib

* Needs:

- wget
- awk filter

* Configuration:

- profile in profiles dir
- awk filter

* Todo:

- dependency support
- improve version checking, most of the filters rely on very simple checks
